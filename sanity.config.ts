// Root config for the Sanity CLI (`sanity dev`, `sanity deploy`, `sanity exec`).
// The real config lives in src/sanity/config.ts and is also used by the embedded
// Studio at /studio.
export { default } from "./src/sanity/config";
